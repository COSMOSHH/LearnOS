# 学习辅助 Agent 项目改造计划

## 1. 文档目的

本文档基于当前仓库代码现状，给出一个面向“个人学习 + 求职展示”的学习辅助 Agent 改造方案。

目标不是简单把“旅行助手”改名，而是把现有项目升级为一个：

1. 能接收当天待学习文档并做 RAG 问答的学习系统。
2. 能在对话中自动带出过往学习内容进行复习的记忆型 Agent。
3. 能体现多 Agent、工作流编排、记忆管理、评测闭环、可观测性与工程化能力的作品。
4. 能作为投递 Agent 应用开发岗位时的代表项目。

## 2. 当前项目现状分析

### 2.1 当前项目本质

当前仓库本质上是一个“旅行场景的多 Agent + RAG + 工具调用 + 会话状态持久化”项目，核心能力已经具备：

1. 多 Agent 路由与栈式切换。
2. RAG 文档入库、分块、向量检索、BM25 混合召回、重排。
3. 基于 FastAPI 的流式接口输出。
4. 基于 SQLite 的聊天历史与线程状态持久化。
5. Human-in-the-loop 的敏感工具拦截思路。

### 2.2 当前可直接复用的模块

以下模块非常适合保留并改造：

1. `Zero_RAG/RAG/document_loader.py`
   价值：已有文档加载能力，可作为学习资料导入入口。
2. `Zero_RAG/RAG/text_splitter.py`
   价值：已有文本切分逻辑，可继续用于知识切片。
3. `Zero_RAG/RAG/vector_store.py`
   价值：已有 Chroma 向量库封装，可继续作为知识库底座。
4. `Zero_RAG/RAG/hybrid_retriever.py`
   价值：已有 BM25 + 向量 + rerank 的混合检索框架。
5. `Zero_RAG/chat_history_service.py`
   价值：已有会话状态与历史记录持久化能力，适合扩展成“学习记忆系统”。
6. `Zero_RAG/llm_generator.py`
   价值：已有流式生成、工具调用能力，可继续作为统一 LLM 适配层。
7. `Zero_RAG/agent_engine.py`
   价值：已有栈式多 Agent 调度思路，非常适合重构为学习场景的工作流引擎。
8. `Zero_RAG/Server.py`
   价值：已有 API 入口，可延展成学习助手后端服务。
9. `Zero_RAG/Client.py`
   价值：已有 Streamlit 原型界面，可快速改造成学习工作台。

### 2.3 当前需要替换或弱化的部分

以下部分与目标场景耦合较深，建议逐步移除或重写：

1. `tools/flights_tools.py`
2. `tools/hotels_tools.py`
3. `tools/car_tools.py`
4. `tools/trip_tools.py`
5. 旅行相关的 SQLite 数据与 FAQ 业务资料。
6. `base_data_model.py` 中围绕旅行委派的工具 schema。
7. 各类旅行领域 system prompt。

### 2.4 当前项目的短板

如果要拿去投递 Agent 岗位，当前项目还缺少几块特别关键的“展示价值”：

1. 没有真正的“长期记忆”分层设计，当前更多是聊天历史堆叠。
2. 没有围绕“学习”构建用户价值闭环。
3. 没有评测体系，难以证明 Agent 质量。
4. 没有任务执行追踪、步骤日志、失败恢复等工程化特征。
5. 没有可视化学习进度、掌握度、复习计划等产品层能力。
6. 没有对“为什么这像一个 Agent 系统而不是普通聊天机器人”做充分体现。

## 3. 项目改造目标

建议把项目定位为：

**一个面向个人知识学习与面试准备的多 Agent 学习操作系统。**

它至少覆盖三类核心场景：

1. 当日学习
   用户上传今天要学的 PDF、Markdown、Word、网页摘录、笔记，系统完成入库、总结、问答、生成重点。
2. 学后巩固
   系统在对话中自动调取过往知识点，以“轻复习”方式主动插入。
3. 长期成长
   系统持续跟踪掌握度、错题、薄弱点、复习节奏和学习计划。

建议项目名称方向：

1. `Study Agent`
2. `Learning Copilot`
3. `StudyFlow Agent`
4. `LearnOS`
5. `Memory Tutor Agent`

如果考虑投递展示，我更推荐偏工程感的命名，例如 `LearnOS` 或 `StudyFlow Agent`。

## 4. 基础功能设计

结合你的要求，基础功能建议定义为以下 MVP：

### 4.1 功能一：接收当天待学习文档并完成 RAG 学习问答

目标：
用户每天上传学习资料后，可以围绕这些资料持续提问、理解、追问、举例、对比和总结。

建议能力：

1. 支持上传 `pdf/docx/txt/md`。
2. 支持按“学习批次”或“学习日期”组织知识库。
3. 文档入库后自动生成：
   - 文档摘要
   - 核心概念
   - 关键术语
   - 可能考点
4. 问答时优先检索“当天学习资料”。
5. 回答时展示引用来源和片段。

### 4.2 功能二：对话时自动出现之前的学习内容进行复习

目标：
让 Agent 不只是答题，还能像一个真正的老师一样，在合适的时候提醒你复习。

建议实现方式：

1. 建立“复习记忆池”，存储：
   - 历史学习主题
   - 核心知识点摘要
   - 用户曾答错的问题
   - 重要结论
   - 易混淆概念
2. 每次对话前，额外执行一次“复习检索”：
   - 与当前问题语义相关的旧知识
   - 到期该复习的知识
   - 用户最近经常遗忘的知识
3. 以低打扰方式注入回答，例如：
   - “顺带帮你复习一下，之前学过……”
   - “这和你 4 月 20 日学过的……有关”
   - “你上次在这个概念上有混淆，注意……”
4. 不要每次都强插复习，避免干扰主任务。
   建议只插入 1 到 3 条，且由策略控制触发频率。

## 5. 推荐扩展功能

为了让项目更完整、更像一个高质量 Agent 应用，建议加入以下功能。

### 5.1 学习规划能力

1. 根据目标岗位或考试方向生成学习计划。
2. 根据剩余时间自动拆分今日任务。
3. 标记“必学 / 可略读 / 需复习 / 已掌握”。

### 5.2 主动回忆与测验能力

1. 自动从文档中生成问答题。
2. 自动生成选择题、判断题、简答题。
3. 让用户先回答，再由 Agent 评分和讲解。
4. 自动形成错题本。

### 5.3 闪卡与间隔复习

1. 自动把知识点生成 flashcards。
2. 引入简单版遗忘曲线策略。
3. 根据答题表现动态调整复习优先级。

### 5.4 概念图谱与知识关联

1. 自动抽取概念、定义、关系。
2. 形成“概念图”或“知识树”。
3. 回答问题时不仅给答案，还指出它在知识图谱中的位置。

### 5.5 错误纠偏与薄弱点分析

1. 识别用户经常混淆的概念。
2. 归纳高频错误原因。
3. 给出“下一步建议学什么”。

### 5.6 学习成果沉淀

1. 每日学习报告。
2. 每周复盘报告。
3. 自动沉淀“面试速记卡”。
4. 自动导出 Markdown 学习笔记。

### 5.7 面试模式

这个功能非常适合你求职展示。

1. 支持切换成“面试官模式”。
2. 基于已学资料出面试题。
3. 对回答进行追问。
4. 输出答题评分、改进建议、标准答案结构。

### 5.8 编程学习增强

如果你希望项目更贴近 Agent 应用开发岗位，建议加入：

1. 代码片段讲解。
2. 接口设计问答。
3. 架构对比分析。
4. 结合本地项目代码做“代码学习助手”。

这会让项目从“学习资料问答”升级成“知识 + 代码 + 面试训练”一体化系统。

## 6. 目标架构建议

建议把系统分成五层：

### 6.1 接入层

负责与用户交互：

1. Streamlit 页面，快速迭代。
2. 后续可升级到 React 前端。
3. FastAPI 作为统一后端入口。

### 6.2 知识层

负责管理学习资料：

1. 原始文档存储。
2. 切片文本存储。
3. 向量库。
4. 关键词索引。
5. 文档摘要与知识点抽取结果。

### 6.3 记忆层

这是本项目最关键的升级点。

建议把记忆拆成四层：

1. 会话记忆
   当前对话上下文。
2. 学习记忆
   某次学习批次的摘要、重点、术语、问题。
3. 复习记忆
   需要周期性召回的知识点、错题、薄弱点。
4. 用户画像记忆
   用户学习偏好、目标岗位、掌握程度、常见误区。

### 6.4 Agent 层

建议不要一开始就造很多 Agent，先控制在 4 到 6 个：

1. `orchestrator_agent`
   总控，理解用户意图，分配子任务。
2. `ingest_agent`
   负责导入、切分、摘要、知识点抽取。
3. `tutor_agent`
   负责问答、讲解、举例、类比。
4. `review_agent`
   负责复习内容召回、错题提醒、间隔复习。
5. `planner_agent`
   负责学习计划和任务拆解。
6. `evaluator_agent`
   负责出题、评分、识别薄弱点。

### 6.5 观测与评测层

这是求职展示的加分项：

1. 每次 Agent 执行保留 trace。
2. 保存检索结果、选中的上下文、最终回答。
3. 记录失败步骤与异常原因。
4. 建立一套离线评测集。

## 7. 面向当前代码的重构建议

### 7.1 优先保留的底座

建议保留以下底座，减少重写成本：

1. `Zero_RAG/RAG/*`
2. `Zero_RAG/chat_history_service.py`
3. `Zero_RAG/llm_generator.py`
4. `Zero_RAG/agent_engine.py`
5. `Zero_RAG/Server.py`
6. `Zero_RAG/Client.py`

### 7.2 优先替换的业务模块

1. 删除旅行工具注册与旅行 schema。
2. 新建学习工具模块，例如：
   - `tools/learning_ingest_tools.py`
   - `tools/review_tools.py`
   - `tools/quiz_tools.py`
   - `tools/progress_tools.py`
3. 将 `base_data_model.py` 重构为学习场景工具 schema。
4. 将 `agent_registry` 中的旅行 agent 改为学习 agent。

### 7.3 建议新增的数据表

当前只有聊天历史和线程状态，不够支撑学习产品。

建议新增：

1. `study_sessions`
   记录某天/某批次学习任务。
2. `study_documents`
   记录文档元数据、来源、标签。
3. `knowledge_points`
   存储抽取后的知识点。
4. `review_items`
   存储待复习条目、到期时间、复习次数、掌握度。
5. `quiz_records`
   存储测验结果、错题、评分。
6. `learning_profiles`
   存储用户目标和偏好。

## 8. “自动复习”机制设计

这是本项目最有差异化价值的部分，建议重点做精。

### 8.1 复习触发策略

每次用户发起对话时，系统并行执行两类检索：

1. 主检索
   从当前学习资料中检索与问题最相关的内容。
2. 复习检索
   从历史学习记忆中检索：
   - 语义相关旧知识
   - 到期复习知识
   - 最近答错的知识

### 8.2 复习打分策略

每个复习条目可以维护一个综合分数：

`review_score = relevance + due_score + error_weight + forgetting_weight`

其中：

1. `relevance`
   与当前提问的相关度。
2. `due_score`
   是否到了复习时间。
3. `error_weight`
   用户是否曾在该知识点上出错。
4. `forgetting_weight`
   根据复习间隔和历史表现估算遗忘风险。

### 8.3 复习输出策略

建议回答模板分为三段：

1. 先回答当前问题。
2. 再补充引用依据。
3. 最后附上“顺带复习”。

这样可以保证产品体验不会偏离用户主问题。

## 9. 可以引入的 Harness Engine 思维

这里我先做一个明确假设：

**我把你说的 “harness engine 思维” 理解为：把 Agent 看成一个可编排、可追踪、可评测、可恢复的任务执行引擎，而不是一次性聊天调用。**

在这个理解下，这个项目非常适合引入以下设计思想。

### 9.1 把一次学习行为视为一个任务运行 Run

每次“导入文档 -> 摘要 -> 抽取知识点 -> 建立复习项 -> 用户问答 -> 输出结果”，都不是散乱动作，而是一条完整运行链路。

建议引入：

1. `run_id`
2. `step_id`
3. `step_type`
4. `step_input`
5. `step_output`
6. `step_status`
7. `started_at / finished_at`

这样你就拥有了一个轻量级执行引擎雏形。

### 9.2 把 Agent 工作流显式化

建议把核心流程拆成可观测步骤：

1. `load_documents`
2. `chunk_documents`
3. `embed_documents`
4. `extract_knowledge_points`
5. `generate_summary`
6. `create_review_items`
7. `retrieve_context`
8. `retrieve_review_memory`
9. `answer_question`
10. `evaluate_answer`

每一步都可以单独记录输入输出、耗时、异常。

### 9.3 引入“执行结果评估”闭环

一个像样的 Agent 项目，不应该只跑通，还应该知道跑得好不好。

建议加入两类 evaluator：

1. 检索评估
   检查召回内容是否覆盖正确答案关键点。
2. 回答评估
   检查回答是否忠于资料、是否清晰、是否引用充分。

你甚至可以引入 `LLM-as-a-judge` 作为辅助评分器。

### 9.4 支持失败恢复与重试

例如：

1. 某文档解析失败，只重跑解析步骤。
2. 向量化失败，只重跑 embedding 步骤。
3. 知识点抽取失败，只重跑抽取步骤。

这会让你的项目明显区别于普通 demo。

### 9.5 支持“人机协作插槽”

比如在以下节点允许人工确认：

1. 文档分类结果不确定时。
2. 知识点抽取质量过低时。
3. 自动生成的复习卡片太泛时。
4. 测验评分结果需要人工校正时。

这和当前项目已有的 HITL 思路是天然兼容的。

### 9.6 支持实验配置与 A/B 对比

你可以把以下参数做成可配置实验项：

1. chunk size
2. top_k
3. rerank model
4. 复习触发阈值
5. 记忆注入策略
6. 回答 prompt 版本

这样你就能展示“不是只会写 Agent，还会调优 Agent 系统”。

## 10. 为求职展示设计的亮点功能组合

如果目标是投递 Agent 应用开发岗位，我建议项目最终至少覆盖以下亮点：

1. 多 Agent 编排。
2. RAG 问答。
3. 分层记忆。
4. 主动复习。
5. 出题测验。
6. 结果评估。
7. 执行链路追踪。
8. Human-in-the-loop。
9. 学习进度可视化。

这套组合比单纯“上传文档问答”更有说服力。

## 11. 分阶段实施路线

### 第一阶段：完成可演示 MVP

目标：
先把“今天就能演示”的学习 Agent 跑起来。

内容：

1. 去掉旅行工具与 prompt。
2. 保留现有 RAG、流式输出、线程状态。
3. 支持上传学习文档并入库。
4. 支持基于当天文档问答。
5. 支持简单历史复习注入。
6. Streamlit 页面改成学习助手界面。

预期结果：
一个能稳定演示“上传资料 -> 提问 -> 回答 -> 自动提醒复习”的版本。

### 第二阶段：补齐学习闭环

内容：

1. 抽取知识点与摘要。
2. 生成复习卡片。
3. 生成测验题。
4. 引入错题本与掌握度。
5. 生成每日学习报告。

预期结果：
项目从“问答工具”升级为“学习产品”。

### 第三阶段：补齐 Agent 工程化能力

内容：

1. 引入 run/step 执行日志。
2. 引入失败恢复机制。
3. 引入评测集。
4. 引入回答质量评分。
5. 支持实验配置。

预期结果：
项目从“产品 demo”升级为“工程化 Agent 系统”。

### 第四阶段：补齐求职展示包装

内容：

1. 写项目架构文档。
2. 输出系统流程图。
3. 准备 demo 脚本。
4. 准备指标展示：
   - 检索命中率
   - 复习召回命中率
   - 回答引用率
   - 用户测验正确率提升
5. 准备简历话术与面试问答。

## 12. 最小可落地版本建议

如果我们优先考虑“尽快做出能投递的版本”，我建议第一版只做下面 6 个功能：

1. 上传当天文档。
2. 文档入库与混合检索。
3. 学习问答。
4. 自动复习注入。
5. 自动生成知识点摘要。
6. 自动生成测验题与错题记录。

这是性价比最高的一组。

## 13. 风险与注意事项

1. 不建议一开始把 Agent 数量做太多，否则会变成过度编排。
2. 自动复习必须低打扰，否则用户会觉得啰嗦。
3. RAG 问答和复习注入要分开检索，否则上下文容易互相污染。
4. 记忆系统不能只存原始聊天，要存结构化学习对象。
5. 项目要尽量保留“工程证据”，如 trace、评测、数据表、执行日志。

## 14. 我建议的最终方向

如果从“用户价值 + 工程含金量 + 求职展示”三个维度综合看，我建议你把这个项目做成：

**一个具备 RAG、分层记忆、主动复习、测验评估、任务执行追踪的多 Agent 学习系统。**

它的定位不只是“会回答问题”，而是：

1. 会组织知识。
2. 会追踪你的学习过程。
3. 会在合适的时候主动复习。
4. 会评估你学得怎么样。
5. 会把一次学习变成可执行、可回放、可优化的 Agent 工作流。

---

如果继续往下做，下一份最值得产出的文档建议是：

1. `产品功能清单 + 优先级`
2. `数据库表结构设计`
3. `多 Agent 架构图`
4. `第一阶段实施任务拆解`

这四份一旦定下来，项目就可以正式进入开发阶段。
