# LearnOS 项目面试高频追问速记版

## 1. 项目定位

### 1. 你这个项目和普通 RAG 项目最大的区别是什么？
- 不是只做“上传文档后提问”，而是做了完整学习闭环。
- 主链路是：资料导入 -> RAG 问答 -> 测验评分 -> 错题回流 -> 复习调度 -> 学习计划 -> 学习报告。
- V2 又补了回答评测、模拟面试和运行观测。

### 2. 你在项目里主要负责什么？
- 基本是独立完成。
- 包括产品设计、数据建模、后端接口、前端工作台、RAG 链路、测验与复习系统、评测和面试模块，以及测试和文档。

### 3. 这个项目解决了什么问题？
- 把零散学习资料组织成可持续学习的状态化系统。
- 不只是“能问”，还能“能测、能复习、能复盘、能练面试”。

## 2. 技术选型

### 4. 为什么选 FastAPI？
- 接口开发快，结构清晰。
- 比较适合流式输出和 AI 后端接口。
- 后续如果切前后端分离也容易延续。

### 5. 为什么选 Streamlit？
- 为了快速验证 AI 产品闭环。
- 开发成本低，能把重点放在业务链路和状态设计上。
- 适合原型、演示和迭代。

### 6. 为什么选 SQLite？
- 单机学习场景足够。
- 建模、调试、迁移都很轻量。
- 很适合当前这个“复杂状态、多表关联、单机演示”的项目阶段。

### 7. 为什么选 ChromaDB？
- 轻量，接入快。
- 足够支撑当前会话级向量检索。
- 适合快速做 RAG 原型和可演示系统。

## 3. 架构设计

### 8. 整体架构怎么说？
- 前端：Streamlit 工作台。
- 后端：FastAPI。
- 结构化状态：SQLite。
- 向量检索：ChromaDB。
- 业务拆分：document、review、quiz、plan、report、evaluation、interview、observability 等 service。

### 9. 为什么要拆这么多 service？
- 为了让每个模块职责明确。
- 避免把问答、测验、计划、面试等逻辑全部堆在一个文件里。
- 后续新增能力时更容易扩展和测试。

### 10. 这个项目为什么说是“状态化”的？
- 因为学习过程里的对象都被持久化了。
- 比如学习会话、复习项、错题状态、学习计划项、面试轮次、回答评测、运行日志。
- 它不是一次性请求，而是可持续演进的学习状态系统。

## 4. RAG 与资料导入

### 11. 文件和网页导入为什么要统一链路？
- 统一成“原始资料 -> 纯文本 -> 切块 -> 向量 -> 摘要/知识点/复习项”。
- 这样新增资料来源时，只需要补解析层，不需要重写后面的学习链路。

### 12. 网页导入的难点是什么？
- 不是“能不能抓”，而是“能不能抽干净正文”。
- 技术博客类页面往往混有导航、目录、侧栏、广告，需要正文抽取和清洗。

### 13. RAG 怎么避免不同学习会话串资料？
- 所有切片 metadata 都绑定 `session_id`。
- 检索时只查当前会话。
- 结构化数据如知识点、复习项、计划、面试也都和 `session_id` 关联。

## 5. 测验、错题、复习、计划

### 14. 为什么要做测验模块？
- 因为问答只能说明系统能答，不代表用户掌握了。
- 测验可以识别薄弱点，给后续错题回流和复习调度提供依据。

### 15. 为什么支持单选、填空、简答三种题型？
- 单选适合概念辨析。
- 填空适合关键术语记忆。
- 简答适合考理解和表达。
- 三种题型组合更符合真实学习过程。

### 16. 错题回流怎么做？
- 提交测验后拿到逐题分数、反馈和建议。
- 低分题自动转成高优先级复习项，进入复习系统。
- 不是测完就结束，而是能继续进入后续学习流程。

### 17. 复习调度为什么有价值？
- 因为不是所有复习项都应该同样对待。
- 我会根据优先级、掌握度、错误次数、到期状态排序队列。
- 再通过 Again/Hard/Good/Easy 更新后续复习安排。

### 18. 学习计划为什么要持久化？
- 因为计划如果只是临时文本，就没法勾选完成和持续跟踪。
- 持久化后可以保存、加载、只看未完成项，并按当前状态重排优先级。

## 6. V2 能力

### 19. 回答评测为什么要做？
- 用户不仅需要答案，还需要知道答案讲得怎么样。
- 所以我从准确性、依据性、完整性、清晰度四个维度评估回答质量。

### 20. 模拟面试和普通聊天有什么区别？
- 模拟面试不是普通问答，而是带轮次、带状态的过程。
- 我拆成 `interview_session` 和 `interview_turn` 两层。
- 每轮会评分、生成追问，最后输出总结。

### 21. 运行观测为什么重要？
- AI 系统很容易“能跑但不好排查”。
- 所以我做了事件日志和 run/step 级日志。
- 出问题时能看到到底卡在检索、生成还是评测阶段。

## 7. 难点与优化

### 22. 项目里最难的不是哪一项技术？
- 不是某个单点技术。
- 最难的是把多个学习环节串成稳定闭环，并保证状态能相互回流。

### 23. 这个项目最大的技术取舍是什么？
- 优先保证链路完整和状态清晰，而不是一开始追求复杂工程化。
- 所以前端先用 Streamlit，数据库先用 SQLite，先把核心价值做出来。

### 24. 如果继续做下去，下一步怎么演进？
- 学习计划编辑、拆分、进度统计。
- 端到端测试。
- 更强的面试追问与报告。
- 更完整的 Agent 编排。

## 8. 高频收尾回答

### 25. 这个项目最能体现你的什么能力？
- 体现我能把 AI 应用从产品抽象、数据建模、后端服务到前端交互完整落地。
- 不只是会接模型 API，而是能做复杂状态系统和持续迭代。

### 26. 如果只让你讲 20 秒，你会怎么说？
- 我独立做了一个 AI 学习工作台 LearnOS，不只是文档问答，而是打通了资料导入、RAG 问答、测验评分、错题回流、复习调度、学习计划、学习报告，再到回答评测和模拟面试的完整学习闭环。
