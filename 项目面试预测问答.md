# LearnOS 项目面试预测问答

## 1. 项目一句话介绍

### Q1：你这个项目是做什么的？

**简短回答：**  
LearnOS 是一个面向个人学习场景的 AI 学习工作台，我独立完成了从资料导入、RAG 问答、自动测验、错题回流、复习调度、学习计划、学习报告，到回答评测和模拟面试的一整套闭环系统。

**展开回答：**  
它的核心不是单点问答，而是把学习过程拆成多个状态化环节。用户可以导入本地文件或网页资料，系统会自动抽取正文、切块、入向量库、生成摘要和知识点，然后围绕当前学习会话进行 RAG 问答。之后系统还会自动生成测验，把低分题沉淀成高优先级复习项，再结合复习调度生成学习计划和学习报告。V2 里我又补了回答质量评测、模拟面试和运行观测，让它更像一个完整的学习产品，而不是一个单纯的聊天机器人。

### Q2：你为什么想做这个项目？

**简短回答：**  
我想做一个比“AI 问答”更完整的学习系统，真正覆盖学习、练习、复习和复盘。

**展开回答：**  
很多 AI 项目停留在“上传文档然后提问”这一步，但真实学习过程里，问答只是一个环节，后面还有自测、错题管理、复习优先级、学习计划和复盘总结。我希望把这些环节串起来，验证自己是否能从产品思路、数据模型、后端服务到前端交互完整落地一个有持续使用价值的系统。

### Q3：这个项目最核心的亮点是什么？

**简短回答：**  
最核心的亮点是我把学习过程做成了一个状态化闭环，而不是只做了一个 RAG Demo。

**展开回答：**  
第一，系统支持文件和网页双来源资料导入，而且共用一条入库链路。第二，它不是停留在问答，而是进一步做了测验、错题回流、复习调度和学习计划。第三，V2 里增加了回答质量评测、模拟面试和 Agent run/step 级观测，使系统具备更强的可分析性和产品完整度。

## 2. 架构与技术选型

### Q4：为什么选 FastAPI + Streamlit + SQLite + ChromaDB 这套技术栈？

**简短回答：**  
因为这套技术栈非常适合快速迭代 AI 应用原型，同时又足够支撑状态化学习系统的落地。

**展开回答：**  
FastAPI 适合快速搭后端接口，天然支持异步和流式返回。Streamlit 适合快速验证 AI 产品交互，不用花太多成本在前端工程化上。SQLite 对单机学习系统足够，而且方便我快速做数据建模和会话持久化。ChromaDB 则适合作为轻量向量库，能快速支撑 RAG 检索。整体上，这套技术栈让我把更多精力放在业务链路和状态设计上，而不是基础设施搭建上。

### Q5：为什么不直接用现成的重型 Agent 框架？

**简短回答：**  
因为这个项目的核心更偏“状态化学习系统”，不是复杂工具编排，所以我更希望自己掌控服务边界和数据流。

**展开回答：**  
我没有把整个系统建立在一个黑盒 Agent 框架之上，而是把关键能力拆成了明确的 service 层，比如 document、review、quiz、plan、report、evaluation、interview、observability 等。这样做的好处是结构清楚、可测试、可演进，后面新增 V2 能力时也不需要推翻整体设计。

### Q6：你的系统整体架构是怎样的？

**简短回答：**  
整体是“前端工作台 + FastAPI 后端 + SQLite 状态存储 + Chroma 向量检索 + 多个 service 模块”的分层架构。

**展开回答：**  
前端用 Streamlit 做学习工作台，负责资料导入、问答、测验、计划、报告、评测和面试交互。后端是 FastAPI，负责资料入库、问答、测验评分、计划生成等接口。SQLite 负责存储学习会话、文档、知识点、复习项、测验、计划、评测、面试、事件日志等结构化状态。Chroma 用于文档切片向量检索。再往上我按职责拆成多个 service，保证每条业务链路都能单独演进和测试。

### Q7：为什么说这个项目是“状态化”的？

**简短回答：**  
因为我不是把用户输入当成一次性请求，而是把学习过程中的多个对象都建成了可持久化、可演进的状态。

**展开回答：**  
比如学习会话有自己的主题和目标，复习项有优先级、掌握度、下次复习时间，错题有 retrying、improving、mastered 状态，学习计划有完成状态和优先级，模拟面试有 session 和逐轮 turn，问答和面试回答还有独立评测记录。这样系统才能真正支撑“持续学习”，而不是只做一问一答。

## 3. RAG 与资料导入

### Q8：资料导入链路是怎么设计的？

**简短回答：**  
我把文件导入和网页导入统一收敛到一套文本入库链路里。

**展开回答：**  
本地文件支持 pdf、docx、txt、md，网页支持单页正文抽取和同站目录页批量导入。导入后统一做正文提取、文本切块、向量入库、摘要生成、知识点提取和复习项生成。这样后续新增新的资料来源时，只需要补一个“原始资料 -> 纯文本”的适配层，不需要重写后面的学习链路。

### Q9：网页导入为什么值得单独做？

**简短回答：**  
因为很多真实学习资料并不在本地文件里，而在博客、教程网站和专栏网页里。

**展开回答：**  
如果只支持文件上传，系统的适用范围会比较窄。网页导入能把 LearnOS 从“文档问答工具”升级成“多源学习资料工作台”。尤其是技术博客、教程目录页这类内容，本身就很适合进入 RAG 和学习链路。我还做了目录页批量导入，便于把一个知识专题整体导入到同一个学习会话中。

### Q10：RAG 这一块你具体做了什么？

**简短回答：**  
我做了资料切块、向量入库、会话级过滤检索、来源返回和与学习上下文的结合。

**展开回答：**  
文档入库时，我把文本切成语义块并写入 Chroma，同时把 session_id、document_id、source_type 等元数据一起存入向量数据。检索时只查当前学习会话的数据，避免不同主题之间相互污染。问答返回时，我还会把来源信息一起展示，并结合 review_service 注入复习提醒，使回答不仅“能答”，还更贴近学习场景。

### Q11：你怎么保证不同学习会话之间不会串？

**简短回答：**  
核心做法是所有入库和检索都严格绑定 `session_id`。

**展开回答：**  
文档切块入向量库时我会把 `session_id` 写进 metadata，检索时用 session 级过滤条件，只从当前会话的切片中找相关内容。SQLite 里所有知识点、复习项、测验、计划、评测、面试记录也都和 `session_id` 关联，所以整个学习过程天然是按会话隔离的。

## 4. 测验、错题、复习与计划

### Q12：你为什么要做测验，不只做问答？

**简短回答：**  
因为问答只能说明“系统能回答”，测验才能帮助验证“用户是否真正掌握”。

**展开回答：**  
问答更多是被动获取信息，而测验可以把知识转成输出任务，帮助识别用户的薄弱点。我做测验的目的，是把项目从“知识获取工具”推进到“学习效果反馈系统”。这也是后面错题回流、复习调度和学习计划成立的前提。

### Q13：测验系统支持哪些题型？为什么这么设计？

**简短回答：**  
当前支持单选题、填空题和简答题三类混合题型。

**展开回答：**  
单选题适合快速验证概念辨析，填空题适合检验关键术语掌握，简答题适合看是否真的理解原理和场景。三类题型组合起来，比只做简答题更适合学习场景，也更有层次感。前端我按题型分别渲染交互控件，后端则按不同评分方式处理结果。

### Q14：错题回流是怎么做的？

**简短回答：**  
测验提交后，我会把低分题提炼成新的复习项，并设置更高优先级。

**展开回答：**  
每道题评分后都会得到分数、反馈和改进建议。对于低分题，我会把题目、反馈、建议和参考答案要点提炼成 `quiz_feedback` 类型的复习项，写入 review_items，并赋予更高的 priority_score。这样错题就不只是测完就结束，而是能继续进入后续的复习调度和学习计划。

### Q15：复习调度具体怎么实现的？

**简短回答：**  
我给复习项设计了优先级、掌握度、到期时间和状态演进，再用队列策略进行排序。

**展开回答：**  
每个复习项都有 priority_score、mastery_level、review_status、next_review_at、error_count 等字段。系统会综合这些字段生成复习队列，并支持 Again、Hard、Good、Easy 四种反馈来更新状态和下次复习时间。这让复习不再是简单列表，而是变成带调度逻辑的动态系统。

### Q16：学习计划为什么也要持久化？

**简短回答：**  
因为计划如果只是临时文本，就无法支持后续的勾选完成、重排优先级和持续追踪。

**展开回答：**  
我一开始可以只做动态生成，但那样每次刷新都是新的文本，用户无法真正执行。后来我把学习计划拆成 `study_plans` 和 `study_plan_items`，支持保存、加载、勾选完成、只看未完成项，并能根据当前复习状态和错题表现重新排序。这样计划才真正从“建议”变成“可执行任务”。

## 5. V2：回答评测、模拟面试、运行观测

### Q17：你为什么要做回答质量评测？

**简短回答：**  
因为只生成回答还不够，我希望系统还能判断回答质量，帮助用户持续改进表达。

**展开回答：**  
尤其是在学习和面试场景里，用户不仅需要“知道答案”，还需要知道“这个答案讲得好不好”。所以我增加了回答评测模块，从准确性、依据性、完整性、清晰度几个维度对回答进行评分，并给出 strengths、risks 和 next_actions。这让系统从“能答”进一步升级到“能评估回答质量”。

### Q18：模拟面试模式是怎么设计的？

**简短回答：**  
我把模拟面试拆成 interview_session 和 interview_turn 两层状态模型，支持逐轮提问、逐轮作答、自动追问和总结。

**展开回答：**  
系统会基于当前学习会话的知识点和摘要生成面试题 blueprint，然后创建 interview_session，并逐轮插入 interview_turn。每次用户回答后，系统会先做回答评测，再根据得分和 focus 生成 follow-up question。如果轮次结束，则输出一份整体面试总结。这样整个面试过程是状态化管理的，而不是简单地把面试问题塞进聊天窗口里。

### Q19：运行观测这一块你做了什么？

**简短回答：**  
我做了事件日志和 Agent run/step 两层观测，既能看业务结果，也能看执行过程。

**展开回答：**  
最开始只有 event_logs，记录导入、测验、计划、复习等关键动作的成功失败和耗时。V2 我又增加了 `agent_runs` 和 `agent_run_steps`，把一次问答或一次面试拆成 run，再进一步拆成 retrieve、generate_answer、evaluate_answer 等步骤。前端也做了运行观测页，方便查看每次调用的状态和性能，这对排查问题和面试讲项目都很有帮助。

## 6. 难点、优化与取舍

### Q20：这个项目里你遇到的最大难点是什么？

**简短回答：**  
最大的难点不是单点技术，而是如何把多个学习环节稳定地串成一个可持续运转的状态闭环。

**展开回答：**  
如果只是做一个 RAG 问答，难点主要在资料检索；但 LearnOS 里还有测验、错题、复习、计划、报告、评测和面试，这些模块之间是相互影响的。我花了比较多精力在数据建模和服务边界上，保证一个环节的结果能自然流向下一个环节，而不是模块之间割裂。

### Q21：这个项目里你做过哪些重要取舍？

**简短回答：**  
最大的取舍是优先保证链路完整和状态清晰，而不是一开始追求复杂前端或分布式架构。

**展开回答：**  
比如前端我选 Streamlit 而不是 React，是因为我更想快速验证产品闭环；数据库先选 SQLite，是因为单机学习系统完全够用，建模和调试成本低；网页导入优先覆盖静态正文页和目录页，而不是一开始兼容所有动态站点；Agent 这部分我也没有一开始追求复杂编排，而是先把 service 层拆干净。这样能让我更快把主价值做出来。

### Q22：如果数据量变大，或者要多人使用，这套方案会有哪些问题？

**简短回答：**  
SQLite 和 Streamlit 在单机演示阶段很好用，但如果多人并发和数据量显著增加，就需要升级存储和前端方案。

**展开回答：**  
SQLite 更适合单机或轻量场景，多用户高并发下会有写锁和吞吐限制。Streamlit 适合原型和展示，但复杂交互和前端状态管理的上限有限。未来如果做成正式产品，我会把 SQLite 升级成 PostgreSQL，把前端升级成 React 或 Vue，并把异步任务、日志聚合和鉴权体系补齐。

### Q23：你做过哪些工程质量方面的工作？

**简短回答：**  
我补了服务层和接口层测试，并且做了运行日志和删除链路的级联清理。

**展开回答：**  
测试方面，我覆盖了网页导入、测验生成与评分、学习报告、回答评测、模拟面试、删除会话级联清理等关键链路。工程上我还重点处理了会话删除时的脏数据问题，保证测验、计划、评测、面试、event logs、agent runs 都能一起清理，避免状态残留。

## 7. 扩展与未来规划

### Q24：如果继续迭代，你下一步最想做什么？

**简短回答：**  
我会优先补学习计划编辑与进度统计、端到端测试，以及更强的面试追问和评测机制。

**展开回答：**  
当前计划已经能生成和重排，但还缺细粒度编辑、拆分和跨天延续。测试层面还可以继续补端到端测试。面试模式也已经能跑通，但追问逻辑和面试报告还可以做得更细。再往后，我也会考虑把多 Agent 真正纳入主工作流，而不仅仅停留在预留能力上。

### Q25：如果面试官问你，这个项目最能体现你什么能力，你会怎么答？

**简短回答：**  
我会说它最能体现我把一个 AI 应用从产品思路、数据建模、后端服务到前端交互完整落地的能力。

**展开回答：**  
这个项目不是只调用了一个模型接口，而是我围绕真实学习场景做了完整系统设计，包括资料入库、RAG、测验、错题、复习、计划、报告、评测、面试和观测。它体现的是我做复杂业务抽象、设计状态模型、拆服务边界、完成全栈交付和持续迭代的能力。

## 8. 面试时建议优先强调的 5 个点

1. 这不是单纯 RAG Demo，而是完整的学习闭环系统。
2. 文件和网页双来源资料导入共用一条统一入库链路。
3. 测验、错题回流、复习调度、学习计划是项目的核心状态化设计。
4. V2 的回答评测、模拟面试和运行观测显著提升了系统完整度。
5. 你是从产品思路、架构设计、后端实现到前端交互独立完成落地的。

## 9. 面试时不建议只停留在表面的说法

1. 不要只说“我做了一个 RAG 项目”，这样会把项目说小。
2. 不要只列功能，要强调“状态化闭环”和“学习链路打通”。
3. 不要只说用了哪些框架，要说明为什么这么选，以及这些选型如何服务项目目标。
4. 不要把重点全放在前端页面，要多讲数据模型、服务拆分和模块之间的回流关系。
5. 如果被追问难点，不要回答“调用 API 比较麻烦”，要重点讲状态设计、模块协同和工程取舍。

## 10. 1 分钟口述版

LearnOS 是我独立设计并实现的一个 AI 学习工作台，目标不是只做文档问答，而是打通完整学习闭环。它支持本地文件和网页资料导入，进入统一的文本入库和向量检索链路，然后围绕学习会话做 RAG 问答。问答之后，系统还能自动生成混合题型测验，把低分题沉淀成高优先级复习项，再通过复习调度生成学习计划和学习报告。V2 我又扩展了回答质量评测、模拟面试和 Agent run/step 级运行观测。这个项目最能体现的是我从产品抽象、状态建模、后端服务到前端交互，把一个 AI 应用完整落地并持续迭代的能力。
