# LearnOS RAG 优化路线图

## 1. 背景判断

当前 LearnOS 的 RAG 已经不属于最基础的 Naive RAG，而是进入了“早期 Advanced RAG”阶段，已有能力包括：

1. 向量检索 + BM25 混合召回。
2. Rerank 重排。
3. `session_id` 级学习会话隔离。
4. 文本切块与 overlap。
5. 检索结果进入学习问答、学习计划、回答评测和模拟面试链路。

但和更成熟的 Advanced RAG 相比，当前仍缺少几块关键能力：

1. 查询侧优化：Query Rewrite、Multi-Query、问题类型识别。
2. 分块优化：标题增强、语义块、Parent-Child 多粒度检索。
3. 上下文治理：去重、压缩、裁剪。
4. 评测闭环：检索评测集、Recall@k、MRR。
5. 产品化能力：检索调试面板、低质量 query 诊断。

本路线图的目标是把 LearnOS 的 RAG 从“可用”升级成“可解释、可调优、可评测”的学习场景 RAG 系统。

---

## 2. 总体目标

围绕 5 个问题推进：

1. 用户怎么问：提升 Query 质量。
2. 系统怎么切：提升资料切块质量。
3. 系统怎么召回：提升复杂问题召回覆盖率。
4. 系统怎么喂给模型：提升上下文有效性。
5. 系统怎么证明自己变好了：建立评测与观测闭环。

---

## 3. 阶段拆分

## 阶段一：低成本高收益优化

目标：快速提升口语化问题、上下文省略问题和教程类网页资料的检索质量。

### 3.1 Query Rewrite

**目标**

把用户原始问题改写成更适合检索的标准化 query。

**要解决的问题**

1. 用户会问“这个是啥”“为什么这样设计”“它和那个有什么区别”。
2. 这类 query 直接检索容易召回不稳。

**实现建议**

1. 新增 `services/query_service.py`。
2. 增加 `rewrite_query(query, history, session_context)`。
3. 在 `/chat` 检索前先执行一次 query rewrite。
4. 在运行观测中保留：
   - `original_query`
   - `rewritten_query`

**预期收益**

1. 提升口语问题与省略问题的检索稳定性。
2. 降低用户表达方式对召回效果的影响。

**建议优先级**

`P0`

**当前状态**

已完成 `RAG V1.1`

### 3.2 标题增强分块

**目标**

让 chunk 不只是“文本切片”，而是“带结构上下文的学习片段”。

**要解决的问题**

1. 当前 chunk 更偏长度驱动。
2. 教程型网页和技术文章往往高度依赖标题层级。

**实现建议**

1. 给 chunk metadata 新增：
   - `section_title`
   - `heading_path`
   - `chunk_type`
2. 网页优先按 `h1/h2/h3 + 段落` 切分。
3. 文件导入尽量补弱标题识别。

**预期收益**

1. 提升教程类内容的定位能力。
2. 提升 rerank 和回答引用质量。

**建议优先级**

`P0`

**当前状态**

已完成 `RAG V1.1`

### 3.3 检索调试信息可视化

**目标**

让 RAG 检索过程可见、可解释。

**要解决的问题**

1. 当前检索过程对用户和开发者都偏黑盒。
2. 后续调参缺乏直观看板。

**实现建议**

1. 在运行观测页展示：
   - 原始 query
   - 改写 query
   - vector top-k
   - BM25 top-k
   - rerank 后结果
2. 新增“检索调试开关”。

**预期收益**

1. 提高调优效率。
2. 强化项目展示价值。

**建议优先级**

`P0`

**当前状态**

已完成 `RAG V1.1`

---

## 阶段二：Advanced RAG 主体能力

目标：提升复杂问题、比较类问题、总结类问题的召回覆盖率与答案质量。

### 3.4 Multi-Query / 子问题分解

**目标**

对复杂 query 自动扩展成多个检索子问题。

**适用场景**

1. 区别类问题。
2. 原因类问题。
3. 总结类问题。
4. 一问多点的问题。

**实现建议**

1. 在 `query_service` 增加：
   - `expand_query_to_multi_queries()`
2. 每个子问题分别召回，再统一去重和 rerank。

**预期收益**

1. 提升复杂问题的召回覆盖率。
2. 减少“答到一半漏掉关键点”的情况。

**建议优先级**

`P1`

### 3.5 Parent-Child 多粒度检索

**目标**

实现“小块召回，大块生成”。

**要解决的问题**

1. 小 chunk 更容易检准，但上下文不足。
2. 大 chunk 上下文完整，但噪声高。

**实现建议**

1. 入库时同时构建：
   - child chunk
   - parent chunk / parent section
2. 检索时先召回 child。
3. 生成前回填 parent 段落或章节。

**预期收益**

1. 兼顾检索精度和回答完整性。
2. 更适合教程、博客、文档型学习资料。

**建议优先级**

`P1`

### 3.6 上下文去重、压缩与裁剪

**目标**

在进入模型前对上下文做治理。

**要解决的问题**

1. chunk 重复。
2. token 超长。
3. 检回来很多相近内容，浪费上下文窗口。

**实现建议**

1. 新增 `services/context_service.py`。
2. 对 rerank 后结果执行：
   - 语义去重
   - token 限长
   - 长段落压缩
3. 输出最终用于生成的 `final_context`。

**预期收益**

1. 提升回答清晰度。
2. 降低冗余信息干扰。
3. 控制推理成本。

**建议优先级**

`P1`

---

## 阶段三：评测闭环与产品化

目标：从“感觉优化了”升级成“能证明优化了”。

### 3.7 检索评测集

**目标**

建立 LearnOS 自己的小型 retrieval benchmark。

**实现建议**

1. 先建立 30-50 条评测样本。
2. 每条样本包含：
   - `query`
   - `session_id`
   - `gold_document`
   - `gold_section`
   - `gold_chunk`
3. 评估：
   - Recall@k
   - MRR
   - Top-k hit rate

**预期收益**

1. 后续每次改 splitter、rewrite、multi-query 都能做量化验证。
2. 强化项目工程质量。

**建议优先级**

`P1`

### 3.8 RAG 质量面板

**目标**

让检索质量成为产品级可视数据。

**实现建议**

1. 新增一个“RAG 质量”页签或观测区块。
2. 展示：
   - Query Rewrite 命中差异
   - 检索耗时
   - top-k 命中统计
   - 低质量 query 样本

**预期收益**

1. 产品可运营。
2. 便于调优和面试展示。

**建议优先级**

`P2`

### 3.9 学习场景专属检索路由

**目标**

按问题类型切换不同检索策略。

**实现建议**

1. 定义类问题：
   - 单 query + rerank
2. 对比类问题：
   - multi-query
3. 总结类问题：
   - parent-child + context compression
4. 面试类问题：
   - step-back / broader retrieval

**预期收益**

1. 让 LearnOS 的 RAG 更贴合学习产品场景。
2. 提升不同问题类型下的稳定性。

**建议优先级**

`P2`

---

## 4. 推荐实施顺序

建议按以下顺序推进：

1. Query Rewrite
2. 标题增强分块
3. 检索调试可视化
4. Multi-Query / 子问题分解
5. Parent-Child 多粒度检索
6. 上下文压缩 / 去重 / 裁剪
7. 检索评测集
8. RAG 质量面板
9. 学习场景专属检索路由

---

## 5. 当前最值得先做的 3 项

如果当前只做 3 项，我建议优先：

1. `Query Rewrite`
2. `标题增强分块`
3. `检索调试信息可视化`

原因：

1. 实现成本低。
2. 对当前 LearnOS 的学习问答收益直接。
3. 当前已完成，可作为后续 Multi-Query、Parent-Child 和检索评测的基础。

---

## 6. 对简历和项目表达的提升

如果这套路线逐步落地，后续可把 LearnOS 的 RAG 能力描述为：

1. 从基础混合检索升级到 Advanced RAG。
2. 实现 Query Rewrite、Multi-Query、Parent-Child 检索和上下文压缩。
3. 建立可观测、可评测、可持续调优的学习场景 RAG 系统。
4. 面向学习与模拟面试场景设计差异化检索策略。

---

## 7. 一句话结论

LearnOS 当前最缺的不是“换一个更大的向量库”，而是把查询优化、结构化分块、上下文治理和检索评测这四条链路补完整。
